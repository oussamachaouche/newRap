'use strict';

/**
 * commune-reparateur router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::commune-reparateur.commune-reparateur');
